\documentclass[runningheads]{llncs}

% ---------------------------------------------------------------
% Include basic ECCV package

% TODO REVIEW: Insert your submission number below by replacing '*****'
% TODO FINAL: Comment out the following line for the camera-ready version
\usepackage[review,year=2026,ID=*****]{eccv}
% TODO FINAL: Un-comment the following line for the camera-ready version
%\usepackage{eccv}

% OPTIONAL: Un-comment the following line for a version which is easier to read
% on small portrait-orientation screens (e.g., mobile phones, or beside other windows)
%\usepackage[mobile]{eccv}


% ---------------------------------------------------------------
% Other packages

% Commonly used abbreviations (\eg, \ie, \etc, \cf, \etal, etc.)
\usepackage{eccvabbrv}

% Placeholder for numbers not yet available
\newcommand{\todo}{\textbf{??}}

% Include other packages here, before hyperref.
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}

% The "axessiblity" package can be found at: https://ctan.org/pkg/axessibility?lang=en
\usepackage[accsupp]{axessibility}  % Improves PDF readability for those with disabilities.


% ---------------------------------------------------------------
% Hyperref package

% TODO FINAL: Comment out the following line for the camera-ready version
%\usepackage[pagebackref,breaklinks,colorlinks,citecolor=eccvblue]{hyperref}
% TODO FINAL: Un-comment the following line for the camera-ready version
\usepackage{hyperref}

% Support for ORCID icon
\usepackage{orcidlink}


\begin{document}

% ---------------------------------------------------------------
\title{Metric Scale in Image-to-3D Generation Is Delegated to the Depth Anchor}

\titlerunning{Metric Scale Is Delegated to the Depth Anchor}

% TODO FINAL: Replace with your author list.
\author{First Author\inst{1}\orcidlink{0000-1111-2222-3333} \and
Second Author\inst{2,3}\orcidlink{1111-2222-3333-4444} \and
Third Author\inst{3}\orcidlink{2222--3333-4444-5555}}

\authorrunning{F.~Author et al.}

% TODO FINAL: Replace with your institution list.
\institute{Princeton University, Princeton NJ 08544, USA \and
Springer Heidelberg, Tiergartenstr.~17, 69121 Heidelberg, Germany
\email{lncs@springer.com}\\
\url{http://www.springer.com/gp/computer-science/lncs} \and
ABC Institute, Rupert-Karls-University Heidelberg, Heidelberg, Germany\\
\email{\{abc,lncs\}@uni-heidelberg.de}}

\maketitle


\begin{abstract}
Image-to-3D generative models output objects in a canonical unit cube, seemingly
blind to physical size. We show this framing is wrong for depth-conditioned
generators: the generator predicts a \emph{dimensionless} object--scene scale
ratio, and metric scale is \emph{delegated} to the conditioning pointmap. On the
same frozen SAM~3D Objects model, substituting a ground-truth metric pointmap for
the default affine-invariant one drops median isotropic object-size error from
75.2\% to 6.7\% --- with no new parameters. Guided by this analysis, we recover
metric object dimensions from a \emph{single RGB image}: we swap the anchor
to a monocular metric estimator (MoGe-2) and lightly fine-tune the layout pathway,
reaching 17.6\% mean per-axis dimension error across three real-world sources ---
on clean data exceeding the accuracy of the raw anchor itself --- while a jointly
supervised variant of the same recipe reaches 73.1 mAP@IoU$_{0.25}$ on
NOCS-Real275 (61.1 in cross-dataset transfer with NOCS held out), surpassing
RGB-only baselines. Unlike prior
metric layout evaluations, no ground-truth depth is used at training or inference.
\keywords{Metric scale \and Image-to-3D generation \and Monocular geometry}
\end{abstract}


\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/teaser.pdf}
  \caption{\textbf{Metric scale is delegated to the depth anchor.} Left: a
  depth-conditioned image-to-3D generator predicts only a \emph{dimensionless}
  object--scene ratio $\tilde{\mathbf{s}}$; physical units enter exclusively through the
  scene scale $s_\text{scene}$ of the conditioning pointmap
  (Eq.~\ref{eq:delegation}). Swapping the anchor on the \emph{same frozen model} moves
  median size error from 75.2\% (affine MoGe) to 6.7\% (ground-truth depth) with zero
  trained parameters (Table~\ref{tab:oracle}). Right: exploiting delegation --- a metric
  monocular anchor plus light layout fine-tuning approaches the oracle
  isotropic-correction floor (Table~\ref{tab:ladder}), using no ground-truth depth.}
  \label{fig:teaser}
\end{figure}

\section{Introduction}
\label{sec:intro}

Image-to-3D generative models have reached a level of visual fidelity where a single
photograph is sufficient to produce a detailed mesh or Gaussian splat of an object with
plausible geometry and texture. Foundations such as SAM 3D Objects~\cite{sam3d} and
TRELLIS~\cite{trellis} train on millions of synthetic and real objects, learning rich
priors over 3D shape and surface appearance. Yet their outputs live in a canonical unit
cube, $[-0.5, 0.5]^3$: a reconstructed bowl and a reconstructed car are indistinguishable
in scale. Without absolute scale, a 3D generative model is an artist's tool, not a spatial
computing primitive --- robotic grasping, AR object placement, and multi-object scene
reconstruction all require physical units.

The standard reading of this limitation is that the \emph{model} lacks metric awareness,
and that recovering scale requires either category-size priors (which plateau at the
within-category variance), a separate metric-depth network bolted on after the fact,
or expensive backbone retraining. This paper argues for a different reading.

\paragraph{Metric scale is delegated, not absent.}
Depth-conditioned image-to-3D pipelines condition generation on a pointmap --- a dense
per-pixel field of $(x,y,z)$ coordinates --- and normalise the object pose into
scale-shift-invariant (SSI) space using a robust scene statistic of that pointmap.
We show (Sec.~\ref{sec:analysis}, Fig.~\ref{fig:teaser}) that the pose decoder of SAM 3D
Objects \emph{already} inverts this normalisation at inference: predicted object size is
the product of a dimensionless, learned object--scene ratio and the input pointmap's
scene scale. The generator never learns metric scale because it never has to --- scale is
\emph{delegated} to whatever depth anchor conditions the model. The default anchor
(MoGe~\cite{moge}) is deliberately \emph{affine-invariant}, so the delegated scale is
meaningless; this, not a missing capability, is why outputs appear non-metric.

Three controlled experiments substantiate and sharpen this claim, all on the same frozen
model. \textbf{(i)~Oracle:} feeding a ground-truth metric pointmap instead of the MoGe
default reduces median isotropic size error on NOCS-Real275 from 75.2\% to 6.7\% ---
zero trained parameters (Sec.~\ref{sec:oracle}). \textbf{(ii)~Error decomposition:}
across three real-world sources, applying a perfect \emph{isotropic} correction to the
model's predicted dimensions removes nearly all cross-source error (per-axis MAPE
drops to 7.7/14.4/22.5\% on NOCS/Objectron/ARKitScenes); object \emph{proportions} are
already accurate, the single scalar anchor factor is the bottleneck
(Sec.~\ref{sec:decomposition}). \textbf{(iii)~Transfer asymmetry:} on Hypersim, a domain
never used to train any component, size read through the pointmap pathway achieves
32\% error while a head that regresses absolute size from the (scale-free) latent
collapses to over 200\%, reverting to training-set size priors
(Sec.~\ref{sec:transfer}). Together these show \emph{where the metric signal lives}:
anchor object size to the pointmap; do not re-regress it from representations that are
scale-invariant by construction.

\paragraph{Exploiting delegation.}
The analysis prescribes a recipe (Sec.~\ref{sec:method}). First, swap the conditioning
pointmap for a \emph{monocular metric} estimator (MoGe-2~\cite{moge2}); the pipeline is
explicitly designed to consume estimated pointmaps, so this is an in-distribution change,
and it alone improves mixed-source dimension error from 22.3\% to 20.2\%. Second, because
a monocular anchor carries systematic, source- and size-dependent bias (\eg it
over-measures close-range objects by up to $1.76\times$) that the anchor alone cannot
correct, we lightly fine-tune the model's layout pathway --- a compact scale head, the
spatial-latent cross-attention, and the layout branch of the Mixture-of-Transformers
backbone (the shape branch stays frozen) --- so the model learns a per-instance
\emph{correction to its own anchor}. This yields our best model: \textbf{17.6\%} mean
per-axis dimension error (10.3\% median) across NOCS-Real275, Objectron, and
ARKitScenes, approaching the oracle floor of the decomposition ($\sim$15\%). On
NOCS-Real275, the fine-tuned model's isotropic size error (3.9\% median) falls
\emph{below} the 6.7\% the same frozen model achieves with a ground-truth depth
pointmap --- direct evidence that the model corrects anchor bias rather than merely
inheriting the anchor. The same delegation mechanism supports full
metric 3D bounding boxes: a variant of the recipe supervised jointly on pose and size
obtains 73.1 mAP@IoU$_{0.25}$ on NOCS-Real275 from RGB alone, surpassing RGB-only
baselines (NOCSformer 43.5~\cite{omninocs}, Cube R-CNN 14.9~\cite{omni3d}), and still
transfers at 61.1 when NOCS is held out from training entirely --- the matched
cross-dataset comparison.

A practical consequence we highlight for metric scene understanding: prior metric layout
evaluations of image-to-3D systems --- including SAM 3D's own --- use ground-truth depth
as the metric reference at evaluation time. Our entire pipeline, training and inference,
uses \emph{no ground-truth depth}: the metric anchor is estimated from the same single
RGB image. This is the setting in which such systems will actually be deployed.

\paragraph{Contributions.}
\begin{itemize}
  \item \textbf{A delegation analysis of metric scale} in depth-conditioned image-to-3D
        generation: the mechanism (Sec.~\ref{sec:mechanism}), an oracle experiment
        isolating the depth source (75.2\%$\to$6.7\% with zero trained parameters), and
        an error decomposition showing a single isotropic anchor factor dominates
        cross-source dimension error.
  \item \textbf{A transfer-asymmetry result}: pointmap-anchored size transfers to unseen
        domains; absolute-size regression from scale-free latents does not. This settles
        \emph{where} metric supervision should attach.
  \item \textbf{A lightweight recovery recipe} --- metric-anchor swap plus targeted SFT of
        the layout pathway --- that needs no ground-truth depth, no new architecture, and
        no backbone retraining, evaluated as an ablation ladder isolating each ingredient.
  \item \textbf{Evidence the model corrects its anchor}: post-SFT accuracy on clean data
        exceeds the raw metric-anchor oracle, and a jointly supervised variant shows the
        delegation mechanism yields correctly placed full metric 3D boxes.
\end{itemize}

\section{Related Work}
\label{sec:related}

\paragraph{Image-to-3D generative models.}
Large-scale image-to-3D systems have converged on a two-stage latent flow-matching design.
TRELLIS~\cite{trellis} introduces the Structured 3D Latent (SLAT) representation, a sparse
voxel grid carrying DINOv2 visual features; a rectified-flow transformer decodes SLAT tokens
conditioned on a single image, with separate VAE heads producing Gaussian splats or textured
meshes. TRELLIS v2~\cite{trellis2} replaces multiview-supervised features with native O-Voxel
representations across four billion parameters. SAM 3D~\cite{sam3d} extends the paradigm to
natural images with clutter and occlusion: a 1.2B-parameter Mixture-of-Transformers (MoT)
geometry model jointly estimates coarse shape and object layout (pose, translation, scale),
followed by a sparse latent refinement model. All three systems represent objects in a
normalised unit cube. Our starting observation is that in SAM 3D Objects the connection to
physical units nonetheless exists --- through the conditioning pointmap --- and that
understanding this delegation is the key to metric recovery.

\paragraph{Monocular geometry and metric depth estimation.}
ZoeDepth~\cite{zoedepth} fine-tunes a relative-depth backbone with per-domain metric bins;
Metric3D~\cite{metric3d} achieves scale-aware depth through canonical-camera normalisation.
MoGe~\cite{moge} reframes the problem as \emph{affine-invariant} pointmap prediction,
achieving state-of-the-art relative geometry while deliberately leaving global scale and
shift unconstrained; MoGe-2~\cite{moge2} restores metric scale via a decoupled global-scale
head, motivated by the observation that entangling metric scale with relative geometry
destabilises both --- an observation our cross-dataset findings independently corroborate at
the object level. MoGe is the depth front-end of the SAM 3D Objects pipeline we build on.
Our work differs from the metric-depth literature in target and mechanism: we recover
\emph{object-level dimensions through a generative 3D reconstruction}, and we show the
reconstruction model can learn to \emph{correct} the systematic bias of its own monocular
anchor --- something the depth model alone cannot do.

\paragraph{Category-level size and pose estimation.}
NOCS~\cite{nocs} defines the normalised-object-coordinate-space framework for category-level
6-DoF pose and size; NOCS-Real275 is our cleanest evaluation source. Cube
R-CNN~\cite{omni3d} regresses metric 3D boxes directly from RGB across the unified Omni3D
benchmark. OmniNOCS~\cite{omninocs} unifies canonical 3D box annotations across ten sources
(97 categories, 380k images) and provides NOCSformer, a class-agnostic 3D lifting model; we
train and evaluate on its NOCS-Real275, Objectron~\cite{objectron}, and
ARKitScenes~\cite{arkitscenes} subsets. These discriminative methods regress size against
category priors learned in training --- a strategy our own diagnostics show plateaus at the
within-category size variance (an $R^2$ of only 0.35 between category and log-size on
ARKitScenes). Our approach instead measures each instance against a per-image metric anchor,
and uses learned priors only to correct the anchor's residual bias.

\paragraph{Adapting frozen generative models.}
Parameter-efficient adaptation such as LoRA~\cite{lora} and ControlNet~\cite{controlnet}
injects new conditioning or behaviour into frozen backbones, typically for semantic control.
We apply the same philosophy to a metric objective: all shape-generating weights stay
frozen (SAM 3D's own ablations show shape quality does not depend on pointmap conditioning),
and only the layout pathway --- the part of the model that reads the anchor --- is adapted.

\section{Where Metric Scale Lives: A Delegation Analysis}
\label{sec:analysis}

\subsection{Mechanism: the pose decoder delegates scale to the pointmap}
\label{sec:mechanism}

SAM 3D Objects conditions its geometry model on a pointmap
$\mathbf{P} \in \mathbb{R}^{H \times W \times 3}$ produced by MoGe~\cite{moge} (or supplied
externally, \eg from a LiDAR sensor). The pipeline normalises the object into
scale-shift-invariant (SSI) space using a robust scene statistic of the input pointmap,
\begin{equation}
  s_\text{scene} = \operatorname{mean}\bigl(\lvert \mathbf{P} - \operatorname{med}_z(\mathbf{P}) \rvert\bigr),
  \label{eq:scenescale}
\end{equation}
the mean absolute deviation about the median depth, computed \emph{in the pointmap's own
units}. The generator's layout branch predicts a per-axis SSI scale
$\tilde{\mathbf{s}}$ --- a dimensionless object--scene ratio --- and the pose decoder lifts
it back to world units at inference:
\begin{equation}
  \mathbf{d}_\text{pred} \;=\; \mathbf{c} \odot \tilde{\mathbf{s}} \cdot s_\text{scene},
  \label{eq:delegation}
\end{equation}
where $\mathbf{c}$ is the canonical extent of the generated shape. Equation~\ref{eq:delegation}
is the delegation: \emph{predicted object dimensions are directly proportional to the units
of the input pointmap}. If the pointmap is metric, the output is metric; if the pointmap is
affine-invariant --- as MoGe's deliberately is --- the output scale is arbitrary. The model
has no metric knowledge of its own and needs none; it has a well-calibrated notion of
\emph{how large the object is relative to its scene}.

\subsection{Oracle: the same frozen model is metric, given a metric anchor}
\label{sec:oracle}

We test the mechanism end-to-end on the frozen public model with \emph{zero} trained
parameters, varying only the input pointmap. On NOCS-Real275 (the one source with
registered ground-truth depth and intrinsics), we compare the default MoGe anchor against
a ground-truth metric pointmap built from sensor depth, and against MoGe-2 self-calibrated
metric pointmaps as the deployable middle ground. Table~\ref{tab:oracle} reports median
isotropic size error ($\lvert \max\mathbf{d}_\text{pred} - \max\mathbf{d}_\text{GT}\rvert / \max\mathbf{d}_\text{GT}$)
on held-out instances from all three sources.

\begin{table}[t]
\centering
\caption{\textbf{The depth source, not the model, controls metric accuracy.} Median
isotropic size error of the \emph{frozen} pose-decoder pathway under three input pointmaps
(same model, same images, same seeds; held-out instances: 64 NOCS / 163 Objectron /
148 ARKitScenes). GT pointmaps exist only for NOCS. Bias is the median ratio
pred/GT.}
\label{tab:oracle}
\setlength{\tabcolsep}{5pt}
\begin{tabular}{lcccc c}
\toprule
Input pointmap & NOCS & Objectron & ARKitScenes & Overall & Bias (N/O/A) \\
\midrule
MoGe (affine-invariant, default) & 75.2 & 260.9 & 25.6 & 81.1 & 1.75\,/\,3.61\,/\,0.81 \\
MoGe-2 (monocular metric)        & 16.7 & 75.7  & 19.6 & 29.2 & 1.16\,/\,1.76\,/\,0.95 \\
GT depth (oracle)                & \textbf{6.7} & --- & --- & --- & $\approx$1.0 \\
\bottomrule
\end{tabular}
\end{table}

Three observations. First, the ground-truth anchor achieves \textbf{6.7\%} median error with
no metric supervision anywhere in the model --- confirming Eq.~\ref{eq:delegation} carries the
full metric signal. Second, the affine default is not merely noisy but \emph{systematically
biased} per source (up to $3.6\times$ on Objectron), which is what any learned head trained
on top of it must spend its capacity compensating. Third, the deployable monocular anchor
(MoGe-2) recovers most of the oracle gap at room scale but retains a strong
close-range bias ($1.76\times$ on Objectron) --- a \emph{learnable} residual, as its
per-instance log-ratio scatter is small ($\sigma_{\log} \approx 0.09$ on NOCS).

\subsection{Decomposition: one isotropic factor dominates dimension error}
\label{sec:decomposition}

Predicted dimensions factor as proportions $\times$ isotropic scale
(Eq.~\ref{eq:delegation}). To locate the error, we apply a per-instance \emph{oracle
isotropic correction} to the frozen model's predictions --- rescaling each prediction by
the single scalar that matches its true isotropic size, while keeping the predicted
proportions untouched. Per-axis MAPE falls from 19/100/33\% to
\textbf{7.7\,/\,14.4\,/\,22.5\%} (NOCS/Objectron/ARKitScenes): nearly all cross-source
dimension error lives in the one scalar the pointmap anchors, while the generator's
\emph{proportions} are already accurate. This defines an oracle floor of roughly 15\%
overall for any method that fixes only the isotropic factor, and it concentrates the
problem: correct the anchor's scalar bias, per instance.

\subsection{Transfer asymmetry: anchor, don't regress}
\label{sec:transfer}

Should metric size be \emph{regressed} from the model's latent features, or \emph{anchored}
to the pointmap? The latents are scale-invariant by construction (they were pretrained on
canonically normalised shapes), so a regression head can only learn dataset-specific size
priors. We test this on Hypersim~\cite{hypersim} --- a photorealistic indoor domain unseen
by every component --- using ground-truth depth pointmaps to remove anchor-quality confounds
(n=400 instances). Size read through the pointmap pathway (Eq.~\ref{eq:delegation}) achieves
\textbf{32.0\%} MAPE overall and wins in every size bucket; a learned head that regresses
absolute dimensions from pooled latent features (trained on NOCS/Objectron/ARKitScenes)
collapses to 207.8\%, reverting to its training-set size priors (869\% on small objects).
The head's number is out-of-distribution by construction --- that is precisely the point:
\emph{the pointmap pathway transfers; absolute-size regression does not.} Consistent with
this, we find that fine-tuning on ARKitScenes with the \emph{affine} anchor yields metric
gains that do not transfer to external benchmarks (HAMMER, iBims-1), while scale-invariant
shape quality transfers unchanged --- under a non-metric anchor, apparent metric learning is
dataset memorisation.

\paragraph{Design consequences.}
The analysis dictates the method: (1)~make the anchor metric (swap MoGe $\to$ MoGe-2);
(2)~attach supervision to the \emph{dimensionless} SSI pathway so the pointmap keeps doing
the metric lifting; (3)~give the model the anchor's own statistics as features, so it can
learn the per-instance \emph{residual correction} that the raw anchor provably cannot apply
to itself.

\section{Method: Exploiting Delegation}
\label{sec:method}

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/pipeline.pdf}
  \caption{\textbf{Pipeline.} A single RGB image and instance mask condition two paths: the
  frozen MoGe-2 anchor supplies the metric pointmap $\mathbf{P}$ and scene scale
  $s_\text{scene}$ (swappable --- the delegation slot), while the Mixture-of-Transformers
  geometry model predicts canonical shape (frozen branch) and dimensionless layout
  (trained branch). The trained MetricScaleHead summarises shape and anchor statistics
  into a scale token injected into the SLAT refiner's cross-attention (the only trained
  SLAT weights); the MetricScaleDecoder reads the refined latent into metric $[W,H,D]$.
  All metric outputs inherit their units from the anchor via
  $\mathbf{d}=\mathbf{c}\odot\tilde{\mathbf{s}}\cdot s_\text{scene}$.}
  \label{fig:pipeline}
\end{figure}

\subsection{Metric anchor swap}
\label{sec:anchorswap}

We replace the pipeline's MoGe front-end with MoGe-2~\cite{moge2} full-frame metric
pointmaps under self-estimated intrinsics (we verified that supplying ground-truth
intrinsics \emph{hurts} --- forcing an external FOV fights the jointly-estimated depth/FOV
--- so no camera calibration is needed at inference). The pipeline is explicitly designed
to consume estimated pointmaps, so this is an in-distribution substitution, not an
architectural change: object-centric crops and the scene statistic of
Eq.~\ref{eq:scenescale} are derived from the injected pointmap exactly as before.

\subsection{Scale head and token injection}
\label{sec:head}

A compact \emph{MetricScaleHead} ($<$0.2M parameters) summarises the metric evidence into a
single conditioning token. Its input concatenates the pooled 8-dim sparse-structure (SS)
shape latent, the SS generator's 3-dim log-SSI scale features, and the anchor statistics
$\log s_\text{scene}$ and the object's median depth offset $\Delta_z$ --- 13 dimensions
total, all computed by the standard pipeline with no extra forward pass:
\begin{equation}
  \mathbf{s} = \mathrm{MLP}\bigl([\bar{\mathbf{z}}_\text{SS};\, \mathbf{f}_\text{scale};\,
    \log s_\text{scene};\, \Delta_z]\bigr) + \boldsymbol{e}_\text{metric}.
  \label{eq:headeq}
\end{equation}
The token is appended to the spatial-latent (SLAT) decoder's cross-attention context via a
transparent proxy around the condition embedder; a learned modality embedding
$\boldsymbol{e}_\text{metric}$ distinguishes it from image tokens. A
\emph{MetricScaleDecoder} ($<$0.05M) pools the refined SLAT features (scatter-mean over
active voxels) with a linear projection of the scale token and regresses
$[\log W, \log H, \log D]$ in metres. During training, the injected token is dropped
(zeroed) with probability $0.1$, mirroring classifier-free-guidance dropout~\cite{trellis}.

\subsection{Targeted fine-tuning of the layout pathway}
\label{sec:sft}

The SAM 3D geometry model is a Mixture-of-Transformers with parameter-disjoint shape and
layout branches. We freeze the shape branch entirely --- SAM 3D's own ablations show shape
quality does not depend on pointmap conditioning, and we possess only 3D box labels, not
ground-truth shape --- and unfreeze, at discriminated learning rates:
\begin{itemize}
  \item the SLAT cross-attention sublayers (24 blocks, $\sim$101M parameters,
        lr $10^{-6}$ with 1{,}500-step linear warmup), which read the injected token;
  \item the MoT \emph{layout} branch (per-block norms, self-attention, MLP;
        lr $10^{-5}$), which produces the SSI scale the anchor lifts;
  \item the scale head and decoder (lr $10^{-4}$).
\end{itemize}
Routing the two MoT branches by their module keys is essential: an earlier variant that
unfroze the backbone uniformly trained only the tiny layout read-heads quickly while the
layout transformer crawled, plateauing per-axis error $\sim$10 points higher. Because the
backbone runs in bfloat16 and the scale token is unbounded, all unfrozen cross-attention
and norm sublayers are wrapped in an fp32 upcast shim --- without it, training diverges with
NaN attention weights within the first hundred steps. The objective is smooth-L1 on
log-dimensions; the shape branch receives no gradient (verified: exactly zero gradient
norm on all shape parameters). Total trained: $\sim$500M of $\sim$2B pipeline parameters,
on a single A100 for 12 epochs over $\sim$10k object records.

\section{Experiments}
\label{sec:experiments}

\subsection{Setup}
\label{sec:setup}

\paragraph{Data.}
We train on the three real-world OmniNOCS~\cite{omninocs} sources: NOCS-Real275 (tabletop,
6 categories), Objectron (hand-held captures), and ARKitScenes (room-scale furniture),
capped at 3{,}334 records per source with balanced sampling ($\sim$10k total). Held-out
evaluation uses disjoint records: 64 NOCS / 200 Objectron / 200 ARKitScenes instances
(fixed split, seed 0, identical across all rows of every table).

\paragraph{Metrics.}
Mean and median absolute percentage error (MAPE) of predicted vs.\ ground-truth
per-axis metric dimensions $[W,H,D]$, plus per-source breakdowns; and official
symmetry-aware NOCS 3D-box mAP at IoU 0.25/0.5 for full-pose corroboration
(Sec.~\ref{sec:map}). We report both mean and median because the sources have heavy
category tails (Sec.~\ref{sec:failure}). The metric readout uses the same flow schedule
as training (4 sparse-structure / 1 SLAT steps); this is faster than the stock generation
schedule and we state it as part of the protocol, since the metric head does not
transfer zero-shot to feature statistics from longer schedules.

\paragraph{Baselines.}
(1)~\emph{Category-mean prior}: regressing log-size on ground-truth category labels ---
the ceiling for any prior-based method --- yields approximately 13.5/22.2/43.6\% per
source. Notably, adding object--camera distance to this regression improves it by at most
2 points, and category+distance explain only $R^2{=}0.35$ of ARKitScenes log-size
variance: priors saturate, per-instance measurement is required.
(2)~\emph{Depth-bridge}: measure the object directly from the masked MoGe-2 point cloud
(percentile-robust bounding box). This uses the same monocular network as our anchor but
no reconstruction model; it wins at room scale (22.7\% median on ARKitScenes) and fails
close-range (71.7\% on Objectron), motivating a learned combination rather than either
component alone.

\subsection{Main result: the delegation ladder}
\label{sec:ladder}

Table~\ref{tab:ladder} is the paper in one table: the same architecture, data, split, and
schedule, changing one ingredient per row.

\begin{table}[t]
\centering
\caption{\textbf{Ablation ladder} for metric dimension recovery. Same recipe, data, and
held-out split throughout; each row changes one ingredient. Per-axis MAPE (\%),
mean over held-out instances; overall median in parentheses. MAE in cm.}
\label{tab:ladder}
\setlength{\tabcolsep}{4.5pt}
\begin{tabular}{lccccc}
\toprule
Model & NOCS & Objectron & ARKitScenes & Overall & MAE \\
\midrule
Category-mean prior (GT labels)\textsuperscript{$\dagger$}
                             & 13.5 & 22.2 & 43.6 & ---  & --- \\
\midrule
Head + injection, MoGe anchor (affine)   & 7.4 & 17.7 & 34.3 & 22.3\;(11.9) & 10.0 \\
\;\;+ metric anchor (MoGe-2)             & 7.9 & 17.4 & 28.6 & 20.2\;(12.0) & 9.0 \\
\;\;+ layout-branch SFT (full recipe)    & \textbf{4.6} & \textbf{16.5} & \textbf{24.5}
                                         & \textbf{17.6}\;(\textbf{10.3}) & 8.9 \\
\midrule
Oracle iso-correction floor (Sec.~\ref{sec:decomposition})
                             & 7.7 & 14.4 & 22.5 & $\sim$15 & --- \\
\bottomrule
\end{tabular}
\vspace{1mm}
{\small \textsuperscript{$\dagger$}Regression-derived approximation (residual RMSE in
log space), reported per source; it uses ground-truth category labels, which our method
does not.}
\end{table}

The anchor swap alone (row 2$\to$3) improves overall error by 2.1 points, concentrated on
room-scale ARKitScenes (34.3$\to$28.6) --- exactly where the affine anchor's bias was
largest. Layout-branch SFT (row 3$\to$4) contributes a further 2.6 points and brings every
source within a few points of the decomposition's oracle floor. Most tellingly, on the
matched isotropic-median statistic of Table~\ref{tab:oracle}, \textbf{the fine-tuned
model reaches 3.9\% on NOCS --- below the 6.7\% the same frozen model achieves with a
ground-truth depth pointmap}: it is not merely inheriting its anchor but correcting the
anchor's per-instance bias using appearance and shape context, which the depth network
alone cannot do. The residual gap to the floor sits in Objectron (close-range anchor
bias) and ARKitScenes category tails (Sec.~\ref{sec:failure}).

\paragraph{Does the model correct the anchor, or just inherit it?}
We test this with a pre-registered analysis: on the same 422 held-out instances, same
precomputed MoGe-2 pointmaps, and same seed, we compare the \emph{raw anchor} (frozen
pose-decoder size readout) against the fine-tuned model, regressing the log size-ratio
$\log(\text{pred}/\text{GT})$ on log object size. The raw anchor has a strong
size-dependent bias --- slope $-0.38$, over-measuring the two smallest size quartiles by
$1.35\times$ and $1.70\times$ (median ratio) --- which a global calibration constant cannot
remove (44.6\% MAPE after removing the best single constant). The fine-tuned model
\textbf{flattens the slope to $-0.07$} and reaches 16.7\% MAPE --- a 27.9-point
improvement over the calibrated anchor (95\% paired-bootstrap CI $[22.8, 33.5]$) --- with
per-source median ratios of 0.97/0.98/1.02 (NOCS/Objectron/ARKitScenes) versus the raw
anchor's 1.16/1.76/0.91. Re-calibrating the fine-tuned model changes nothing (16.9\%),
\ie its residual is no longer a systematic bias. The model has learned a per-instance,
size-dependent correction that no constant --- and hence no post-hoc calibration of the
depth network --- can express. This is the sense in which the contribution is a
\emph{model}, not a depth swap.

\subsection{How should the model read the anchor? Head-representation ablation}
\label{sec:headablation}

With generators frozen (features cached), we compare four scale-decoder designs on
identical inputs and splits: absolute log-dimension regression; the same plus explicit
\emph{anchor features} (the pose-decoder's delegated isotropic size, log pose scale, and
voxel extents); a factored variant with gradient-decoupled isotropic/proportion branches
(after MoGe-2~\cite{moge2}'s decoupled-scale loss); and a binned-classification isotropic
head (after~\cite{omninocs}). Absolute regression plateaus at 35.4\% mean and then
overfits; adding anchor features improves it to \textbf{27.2\%} ($-$8.2 points); factored
(28.3\%) and binned (27.6\%, best median 14.4\%) are statistically indistinguishable from
the anchor-feature model. \emph{Access to the anchor is first-order; the representation of
the correction is second-order} --- converging evidence for the delegation thesis from the
opposite direction. (These cached-feature runs lack the live SLAT capacity of
Table~\ref{tab:ladder} and are mutually comparable only.)

\subsection{Full 3D boxes: mAP corroboration}
\label{sec:map}

Metric dimensions are only useful if they are correctly placed. We evaluate full predicted
3D boxes (predicted rotation, translation, and size; ground-truth 2D instances) with the
official symmetry-aware NOCS mAP on NOCS-Real275, RGB-only at inference.

\begin{table}[t]
\centering
\caption{3D-box mAP on NOCS-Real275. Top: the depth-source A/B on a fixed trained model
(8 frames): the anchor moves mAP from zero to competitive. Bottom: full-set evaluation
(150 frames). ``Sees NOCS'' = whether NOCS-Real275 was in the method's training data:
the Cube R-CNN and NOCSformer rows are \emph{cross-dataset transfer} results
from~\cite{omninocs} (NOCS held out), so they are directly comparable only to our
NOCS-held-out and frozen rows. ``Frozen'' rows use the released model with zero
fine-tuning, changing only the anchor; all ``Ours'' rows are RGB-only at inference.
Full-set rows use a fixed 150-frame subset of
the 2{,}754-frame Real275 test set (seed 42; 896 instances, $\approx$5\%), identical
across our rows. \textsuperscript{$\ast$}Uses depth input at inference.}
\label{tab:map}
\small
\setlength{\tabcolsep}{3pt}
\begin{tabular}{llccc}
\toprule
 & Method / condition & Sees NOCS & mAP@0.25 & mAP@0.5 \\
\midrule
\multirow{3}{*}{\shortstack[l]{Depth-source A/B\\(same trained\\model, 8 frames)}}
 & MoGe anchor (affine)      & \checkmark & 0.0  & 0.0 \\
 & MoGe-2 anchor             & \checkmark & 40.3 & 4.9 \\
 & GT-depth anchor (oracle)  & \checkmark & 89.8 & 17.6 \\
\midrule
\multirow{7}{*}{\shortstack[l]{Full set,\\150 frames}}
 & Cube R-CNN~\cite{omni3d} (RGB)        & --- & 14.9 & 4.1 \\
 & NOCSformer~\cite{omninocs} (RGB)      & --- & 43.5 & 10.6 \\
 & NOCS~\cite{nocs} (RGB-D)\textsuperscript{$\ast$} & \checkmark & 79.6 & 72.4 \\
 & Ours frozen, affine anchor            & --- & 0.1  & 0.0 \\
 & Ours frozen, MoGe-2 anchor            & --- & 57.7 & 3.0 \\
 & Ours, NOCS held out                   & --- & 61.1 & 1.0 \\
 & Ours, joint layout SFT                & \checkmark & \textbf{73.1} & 8.9 \\
\bottomrule
\end{tabular}
\end{table}

The A/B (top of Table~\ref{tab:map}) is the delegation mechanism seen through mAP: with the
affine anchor the boxes are metrically misplaced (53\% translation error $\Rightarrow$ 0
mAP); the monocular metric anchor recovers 40.3; the GT anchor exceeds the
\emph{depth-supervised} NOCS baseline at IoU 0.25. The full-set frozen rows extend the
A/B to the released model: swapping the affine anchor for MoGe-2 lifts mAP@0.25 from 0.1
to 57.7 \emph{with no training at all} --- already above the RGB-only transfer baselines.
Our jointly fine-tuned model reaches
\textbf{73.1 mAP@0.25 from RGB alone}, approaching the depth-input NOCS method, with
median translation error 8.3\,cm. We note a protocol asymmetry the table makes explicit:
the Cube R-CNN and NOCSformer numbers are cross-dataset \emph{transfer} results (NOCS
held out from their training), whereas our 73.1 model saw $\sim$3.3k NOCS records; the
matched comparison is our NOCS-held-out row, trained on Objectron+ARKitScenes only.
That model reaches \textbf{61.1 mAP@0.25} despite never seeing a NOCS image ---
surpassing both NOCSformer's transfer result (43.5) and the zero-fine-tuning anchor swap
(57.7) --- with translation transferring intact (median 10.3\,cm, matching the
NOCS-trained model on the same frames); its mAP@0.5 (1.0) drops below the NOCS-trained
model's, as the size and rotation read-heads were never calibrated to NOCS.
mAP@0.5 remains bounded by $\sim$8\,cm translation precision, which our analysis
attributes to the monocular anchor's depth accuracy, not to size (an oracle study shows
size errors below 30\% do not affect mAP@0.5 given correct pose).

The dimension model (Table~\ref{tab:ladder}) and the box model here are two fine-tunes of
the same backbone: the mAP model co-trains the layout branch against pose+size targets,
while the dimension model trains it only through the metric-head loss, which we find
drifts the frozen pose read-heads' translation calibration ($\sim$0.5\,m error). We
therefore report each model on its own task and leave a single jointly-supervised
checkpoint --- warm-started from the dimension model --- to future work.

\paragraph{Post-hoc geometric refinement obeys the same logic.}
The released pipeline includes a render-and-compare pose refinement step (mask-IoU
gated). On a fixed 8-frame subset (51 instances) it behaves exactly as delegation
predicts: it \emph{helps} the frozen model with the metric anchor ($+4.1$ mAP@0.25,
38.5$\to$42.6), does nothing with the affine anchor (0.0 before and after), and
\emph{hurts} the fine-tuned model ($-6.8$, 67.4$\to$60.6; 25 of 51 translations worsen).
Refinement pulls the pose toward the anchor-and-mask evidence, which helps while
translation is anchor-limited but re-imports the systematic anchor bias that the
fine-tuned translation pathway has already corrected --- the geometric counterpart of
the calibration result in Sec.~\ref{sec:ladder}.

\subsection{Failure modes and limitations}
\label{sec:failure}

We state these explicitly; each is a measured property of the system, not a conjecture.

\paragraph{The headline tracks the anchor.}
By design, accuracy inherits the anchor's failure modes. MoGe-2 over-measures close-range
objects ($1.76\times$ median on Objectron); SFT reduces but does not eliminate this
(Objectron 16.5\% vs.\ floor 14.4\%). Where the anchor collapses entirely, so will the
system.

\paragraph{In-domain anchor caveat.}
ARKitScenes appears in MoGe-2's training mixture (8.3\% weight), so part of our
ARKitScenes gain reflects the anchor's in-domain advantage. NOCS-Real275 and Objectron are
\emph{not} in MoGe-2's training set; results there are genuine out-of-domain transfer for
the anchor.

\paragraph{Amodal ground truth vs.\ visible-region input.}
Roughly 28\% of ARKitScenes objects are frame-clipped: the annotation is a full-object
(amodal) box while the model sees only the visible mask. Category tails (shelves 78\%,
sink 61\% MAPE) concentrate exactly there, inflating the ARKitScenes mean; the median is
robust to this.

\paragraph{Flat-object proportions.}
Laptop is the lone weak NOCS class at mAP@0.25 (21.6 vs.\ 69--96 for other classes) --- a
proportion failure of the shape prior on thin, open objects that the anchor cannot fix,
since only the (frozen) shape model controls proportions.

\paragraph{Evaluation scope.}
Held-out sets are modest (464 instances) and single-seed; mAP is NOCS-only, under the
localisation-given-GT-instances protocol (perfect detection assumed). Variance estimates
and detector-in-the-loop evaluation are future work.

\subsection{Qualitative results}
\label{sec:qualitative}

% TODO: Figure — 4 columns: (input image | predicted 3D box vs GT box overlay |
%   reconstructed mesh at predicted metric scale | failure case).
%   Use variant2_fixture_failures.ipynb renders (pred-box vs GT-box overlays exist).

\begin{figure}[t]
  \centering
  \includegraphics[width=\linewidth]{figures/qualitative.pdf}
  \caption{Predicted metric 3D boxes (orange) vs.\ ground truth (white) on NOCS-Real275,
           RGB-only inference with the MoGe-2 anchor. Panel titles give the class and
           translation error. The first six panels span the six categories; the last two
           show the characteristic failure mode --- distant, partially occluded cameras
           and bottles where the monocular anchor's depth error displaces the box along
           the viewing ray.}
  \label{fig:qual}
\end{figure}

\section{Conclusion}
\label{sec:conclusion}

Metric scale in depth-conditioned image-to-3D generation is not missing --- it is delegated.
The generator predicts a dimensionless object--scene ratio, and the conditioning pointmap
supplies the units; with a metric anchor, a frozen generator is already a metric
reconstruction system to within 7\% on clean data. Once stated, this mechanism organises
everything else: cross-source dimension error collapses into the anchor's single isotropic
factor; size supervision must attach to the anchored pathway, because absolute-size
regression from scale-free latents fails to transfer; and the right role for learning is
the \emph{per-instance correction of the anchor's own bias}, which our targeted layout
fine-tuning achieves --- surpassing the raw ground-truth-anchor oracle on NOCS while using
no ground-truth depth at training or inference. We believe the delegation lens applies to
any generation pipeline that normalises its conditioning by a scene statistic, and that
the anchor-conditions-and-supervises pattern (using the pointmap itself as a label-free
metric target, as MoGe-2 does at the pixel level) is the natural next step toward metric
3D reconstruction that scales beyond 3D-box-annotated data.

\section*{Acknowledgements}
TODO

% ---- Bibliography ----
\bibliographystyle{splncs04}
\bibliography{main}
\end{document}
